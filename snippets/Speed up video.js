document.querySelectorAll("video").forEach(video => video.playbackRate = 2);

//document.querySelector("video").playbackRate = 2;

// Or speed up descendants of the current dom inspection
//$0.querySelector('video').playbackRate = 2;
